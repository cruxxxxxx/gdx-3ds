# RESUME — G-Diffuser 3DS (2026-09-02 evening)

## State: RELEASE-GRADE+. Mainline = `feat/3ds-hwaudio` (see git log; merges 2a84e4f + filelog cap).
Both HW-verdict-gated levers merged after user verification on New3DS:
- **brfast** (bridge per-list memos, `[debug] brfast`, DBG-tab toggle): HW `[prof]` crowd windows
  br 6.43 -> 3.73 ms (-42%), all-window mean 5.18 -> 2.83; other buckets unchanged. Watchdog
  fps floors rose (p10 ~42-48 -> ~47-55). Emu: 11.5 -> 4.9 ms.
- **HOME fix**: suspend/resume clean (audio park gate, per-thread sticky park events, backlight,
  speedup+syscore re-apply); close-from-HOME works (root cause was the drain thread hot-spinning
  on a signaled shared sticky event, starving the same-core producer -> join never returned).
  Flushed `[exit]` receipts around every teardown step.
- Default filelog cap 256 KB -> 1 MB (256 truncated one HW session before the exit receipts).
Desktop package refreshed: `~/Desktop/G-Diffuser-3DS-hardware/` (3dsx + cia + elf + README).
Build: `cd ~/code/gdx-3ds/m1 && export DEVKITPRO=/opt/devkitpro && cmake --build build-3ds -j8`
Fresh worktree: scratch script pattern = `git -c protocol.file.allow=always submodule update --init
--recursive`, apply README patch list, configure with `-DGDX_PLATFORM_3DS=ON`.

## Research verdict (docs/research/render-architecture-alternatives.md)
No N64-decomp port has shipped beyond per-frame DL walking; GPU T&L (our shelved
feat/3ds-gputransform) is the field's #1 pick; bridge OUTPUT cache is a no-go here (87% of
commands host-built per frame). Remaining levers: draw-count reduction (texture atlasing, 137
machine-texture switches/frame), gputransform phase-2 (lit geometry), GBI shim (bridge-only gain).

## Backlog
- Boot audio jitter: 2 underruns in the first second (asset preload thread shares core 2 with
  the audio threads); pre-existing, cosmetic. Candidate: preload on core 1/0 or delay ndsp start.
- gputrace/verbose raise underruns (measurement tax) — documented in README known issues.
- #27 tunnel roof (one course), deprioritized.
- Upstream candidate: lus-cc-key-uninit-shader-id.patch -> Kenix3/libultraship.

## Gotchas added this round
- NEVER overwrite the user's SD gdiffuser.ini with a bare file — merge keys (lost their [stereo]
  enabled=1 once). The menu rewrites the ini; [stereo]/[display]/[perf] live there.
- Luma dumps: sdmc:/luma/dumps/arm11/*.dmp; parser at scratchpad luma_dump.py (recipe in
  docs/research/home-crash-audit.md). Keep the ELF of every build shipped to hardware.
- Azahar cannot test HOME/close/sleep; those are hardware-only rounds.
- Agents end their turn when a Monitor is armed; they must be told to stay in-turn with an
  until-loop, or be re-messaged when the run lands.
