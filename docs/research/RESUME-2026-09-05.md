# RESUME — G-Diffuser 3DS (2026-09-04 late)

## Mainline `feat/3ds-hwaudio` = round 3 merged (HW-verified)
+ render thread on core 2 (`[debug] renderthread`, mode 1: fork/join inside the game's frame
  protocol; DBG row RENDER THR) — HW: stable, HOME/close clean, fps median 49->56 p10 42->48
  (trace on) but main still waits 10-15 ms/frame for the renderer (game waits DP-done of the
  frame it just submitted) -> M6 in progress on feat/3ds-renderthread: `renderthread=2` true
  one-frame-ahead pipeline (deferred DP-done ack, 2-deep backpressure).
+ boot audio v2 (`audioprime2`) + fzerox.o2r with audio_table/common_assets stored
  uncompressed (prebake `--store-entry`; boot preload 4.3 -> 2.0 s on HW; font stall gone).
  USER SDs NEED THE NEW ARCHIVE (release package carries it).
+ console filter devoptab (menu owns the bottom screen: stray stdout/stderr dropped; render
  thread never echoes) — fixed the scrolled-away tab bar on HW.
+ log.txt -> log-prev.txt rotation per boot; build id needs a cmake reconfigure after merges.
Not merged: feat/3ds-dspcensus (census gated; dspfast lever default-off, cannot pay).

## Remaining plan
1. M6 renderthread=2 (the max(logic, render) prize) -> HW A/B.
2. Task E machine texture atlas (per-part texture switches = the remaining crowd batch breaks;
   only CLAMP tiles are atlas-safe; reuse Task A's page/view machinery in gfx_citro3d.cpp).
3. Then re-profile on HW; locked 60 should be within reach in crowds.

## Gotchas (new)
- Agents verify levers singly: combined race screenshot before staging (window capture:
  `swift /tmp/wid.swift` -> `screencapture -x -l <id>`; Azahar layout_option=0 for 2 screens).
- Weak `__thread` externs do not link from every TU: use a plain accessor
  (gdx3ds_rt_on_render_thread) instead.
- `extern "C"` is not allowed at block scope in C++.
- HW A/B needs GPUTRACE on for [prof]/[rt] windows; `[rt] waitMain/waitRender` tell the
  pipelining story directly.
