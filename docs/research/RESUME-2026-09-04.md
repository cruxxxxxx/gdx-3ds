# RESUME — G-Diffuser 3DS (2026-09-03 evening)

## State: mainline `feat/3ds-hwaudio` = release + LOCKED-60 campaign merged (HW-verified)
Campaign levers (all `[debug]` keys, default on; DBG tab LEVERS row flips atlas+tri+tmem, atlas
needs a relaunch): `trectbatch` HUD atlas + texrect batching (+LRU page eviction),
`trifast` s7 memo fix (NEVER hit before: textures_changed flag bug) + packed loop, `tmemfast`
TMEM span fast path (+alignas(8) tmem), `audioprime` ring priming, `brfast` bridge memos.
HW A/B (same course, gputrace on, 3D on): steady race 16.4 -> 14.9 ms CPU/frame, crowd
20.2 -> 18.7; fps median 46 -> 49 (trace tax on), p10 39 -> 42. Four HW rounds zero errors,
HOME suspend/resume/close clean. Log now rotates to log-prev.txt per boot.
Package: `~/Desktop/G-Diffuser-3DS-hardware/` (3dsx + cia + elf + README).

## Where the frame goes now (HW, crowd, ms): dsp ~7.8 tri ~4.6 br ~3.1 drw ~1.2 vtx ~1.2 imp ~0.8
= ~18.7 ms CPU + trace tax; without trace ~57-60 fps median on real play. Locked 60 needs ~2-3
more ms in crowds. Remaining levers, ranked: (1) dsp remainder (per-command dispatch + FA/FB
prim/env flushes + F5 settile ~130/frame) — profile with [profop] on HW; (2) tri 4.6 (8 us/tri
still; per-phase HW census via tri2census=1); (3) machine texture atlas (imp + draw breaks);
(4) 30Hz-logic+interp (last resort, gdx_interp infra exists).

## Backlog
- Boot audio: still under=2 at ~10 s on HW (rmin=320) with audioprime -> move preload thread
  off core 2 (option b in bootaudio-progress.md).
- Atlas: 8-page cap + LRU eviction; copy-out burst at transitions is a one-off hitch.
- #27 tunnel roof; upstream lus-cc-key-uninit-shader-id.patch.

## Gotchas (new)
- Agents verify levers SINGLY; combined-lever visual check (screencapture during the emu smoke,
  race HUD) is mandatory before staging. Atlas x trifast broke the HUD that way.
- Patch stack merges: regenerate later patches as pure deltas via `git merge-file` (ours = stack
  so far, base = m1's patched file, theirs = feature worktree's file); union-resolve same-anchor
  insertions; ALWAYS grep for conflict markers before committing a patch.
- HW crowd-window selection: use vtxN buckets, not nD (the atlas cuts draws 4x).
- `/tmp/hw-art-latest` pointer file matches the `hw-art-*` glob — use `hw-art-1*`.
- Never overwrite the SD ini (merge keys); stereo defaults OFF.
- Emulator fps is meaningless under machine load (builds + Ableton) — check `uptime` first.
