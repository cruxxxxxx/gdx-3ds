# RESUME — G-Diffuser 3DS port (pause 2026-08-21 night, Fable-5 tokens out)

## State: RUNS ON REAL NEW3DS — video + audio + input + perf all working
Branch `feat/3ds-hwaudio` @ `db38c47` = current m1 mainline (all today's work merged).
Build: `cd ~/code/gdx-3ds/m1 && export DEVKITPRO=/opt/devkitpro && cmake --build build-3ds -j8`
CIA target: `cmake --build build-3ds --target G-Diffuser-3DS-cia`
Artifacts fresh in `build-3ds/port/3ds/G-Diffuser-3DS.{3dsx,cia}`.
Desktop package current: `~/Desktop/G-Diffuser-3DS-hardware/` (cia + SD-card/).

## SD loop (hardware test)
Card mounts `/Volumes/NO NAME`. Copy `.3dsx` to `3ds/`, write `3ds/gdiffuser/gdiffuser.ini`,
`rm log.txt`, `sync`, `diskutil eject`. Read back: `3ds/gdiffuser/log.txt`.
INI keys: `filelog=1` (log to SD), `gputrace=1` ([gpu]/[prof]/[profop] profiler — NOW works on HW
via filelog sink), `diag_audio=1` (audio receipts + re-arms rs-cap/pcm-cap spam). Default play ini:
`[debug]\nconsole=0\nfilelog=1`.

## DONE today (all merged, hardware-verified by user)
- AUDIO: was silent by design — 3DS builds `decomp/src/audio/rom/` whose Audio_Update was a stub;
  rom-driver host port (patch decomp-3ds-rom-audio-port.patch) + LINEAR ndsp interp (POLYPHASE
  silent on real DSP, fine in emu). Emu needs dspfirm.cdc on virtual SD too.
- SHIP LIVERY: backend ignored shader-requested clamping → Falcon's own stripe decal tiled 7x.
  Fix f1247a9 force GPU_CLAMP_TO_EDGE on shader-clamped axes. MAY cascade-fix buildings #33 + shadow.
- RACE-START HITCH: 4 gfx segment_blobs first-touch at race entry + ~1050 instrumentation lines w/
  per-line SD fflush on audio thread. Blobs → boot preloader; spam gated behind diag_audio.
- FPS HUD bottom screen; L/R drift + ZL/ZR boost input remap; gputrace HW-visible.

## HW perf profile (real New3DS, watchdog+profiler)
FPS: median 48-50, p95 58, max 59, traffic dips 25-35. Native 60 IN REACH (grind traffic tail).
Worst traffic window (draws~208, wall 50ms=20fps, GPU idle 3ms — CPU bound):
`[prof!] br=13.5-14.1 dsp=8.3-8.5 tri=6.7-7.0 imp=1.4-3.0 drw=2.3-2.4 vtx=1.8`
`[profop] 06(TRI2)=6.9-7.7ms/430  F3(LOADBLOCK)=2.7/270  E7(PIPESYNC)=2.5/450  01(VTX)=2.0  E4(TEXRECT)=1.8`

## NEXT — resume here (traffic-grind agent had NOT started; relaunch it)
1. TRAFFIC GRIND to 60fps (feat/3ds-traffic, off hwaudio). 4 ranked levers:
   - br 14ms: bridge ProcessList per-command micro-opt (reserve vector, kill per-cmd memcpy/alloc;
     NO per-command validity cache — S7 dlcache regressed 2x in emu; if cache, O(1) epoch per DL).
   - E7 PIPESYNC 450/frame: likely Flush()-ing → batch breaks. Make true no-op unless real pending
     state hazard (flush-on-state-change already covers part transitions — verify + cite conditions).
   - 06 TRI2 16us/call: S7 tri-memo likely THRASHING across 30 machines → full-resolve every tri.
     Memo per-machine or widen whitelist for machine-DL pattern. + clip pre-scan (7plane×3vtx switch
     per tri even fully-inside → extend vertex-time clip codes, OR-test 3 bytes).
   - F3 LOADBLOCK 2.7ms: same-content skip hit-rate in cross-machine slot churn.
   Target: worst window 50→~17ms. Verify [prof!]/[profop] emu deltas + no visual diff + or=0xFFFF.
2. SKY WEDGE round 3 (feat/3ds-skywedge3, off hwaudio): USER NEW THEORY — "fog/cloud shader,
   weird texture." Chase CLOUD LAYER (decomp ovl_i3/background.c Background_UpdateCloudVtx: scrolled
   alpha-blended cloud quads) rendering black-opaque post-clamp-fix. KEY CLUE: my scanouts NEVER show
   the wedge user photographs → may live in late compositing/present path (240x400 transfer edges,
   hold-tick recomposite) scanout skips. Discriminators: skip cloud draws (gone?=clouds), force cloud
   combiner red. Old coverage/verticalRange theory = DEAD END.
3. STEREO 3D: foundation merged (dual targets, per-eye loop, off default). Needs per-eye projection
   math + costs CPU headroom (do AFTER 60fps — interp path is stereo-friendly if native falls short).

## Remaining visual backlog
#29 sky wedge (above), #31/#32 shadow shape+boost XLU, #33 buildings (re-verify post-clamp-fix),
#36 ccmux 11 explosion combiner unmapped, #27 tunnel roof (deprioritized).

## Process lessons (burned in)
Agents park on stale monitor timers — that's normal, ignore. NEVER kill agent on partial narration
(cost 3h + misattribution in the audio saga). Bind every audible/visual verdict to a VERIFIED
binary (md5). Scripted-run silence needs race-reached + long-soak or it's a phantom. Two sessions in
one worktree = collision (audio forensics). Azahar undersells HW + fakes DSP/threading — HW is truth.
