# RESUME — G-Diffuser 3DS (pause 2026-08-28, next Fable-5 credits ~Sep 2)

## State: RELEASE-GRADE. Mainline = `feat/3ds-hwaudio` @ `a9eb47f` (clean-verified from pristine).
Full patch stack applies from clean submodules; `.3dsx` + `.cia` build green. Desktop package
current: `~/Desktop/G-Diffuser-3DS-hardware/` (3dsx + cia + README-3DS.md).
Build: `cd ~/code/gdx-3ds/m1 && export DEVKITPRO=/opt/devkitpro && cmake --build build-3ds -j8`
CIA: `cmake --build build-3ds --target G-Diffuser-3DS-cia`.

## HW status (user-verified New3DS)
Median ~51 fps, p95 60, max 60 — TOUCHES AND HOLDS 60. Crowds w/ RIVAL DETAIL: MINIMAL floor ~40
(was 25-35). Working: video, audio (music+SFX), stereo 3D (slider, tunable), input+remap, touch
menu, clean POWER/HOME/sleep, flat heap (no leak), all transitions. Zero known crashes.

## THE FINAL LEVER — resume here: bridge translation cache
Biggest remaining fps bucket = bridge command pre-pass ~11.5ms/crowd-frame (ProcessList/ConvertRoot).
Agent feat/3ds-bridgecache DIED on token limit with 0 commits (nothing to recover — worktree removed).
RELAUNCH fresh. Design (full brief in this session's history + memory):
- O(1)-per-DL validity ONLY (per-command validity REGRESSED 2x in Azahar — hard constraint).
- Cache key = DL root ptr + global EPOCH bumped on DMA-into-RDRAM / segment-table change / asset
  catalog change. Validity = one epoch compare per cached DL + DMA dirty-range overlap per cached
  DL source range (the [wide] cache ALREADY does exactly this range-overlap validity — study its
  design in port/n64_gfx_bridge.cpp; [wide] census shows hit~3000/frame rb=0 in-race = DL content
  stable frame-to-frame, so caching is viable).
- Cache value = ConvertedList output (reuse the 4MiB recycle pool). Cache unit = the gdl (sub-)DL
  boundary ([race-dl] lists=14-86/frame = natural unit).
- CLASSIFY commands: translation bakes segment-RESOLVED pointers (safe if segment change bumps
  epoch) vs copies VALUES (can't cache — patch-slot or exclude). This classification IS the risk.
- Killswitch [debug] bridgecache=0 mandatory; [bridgecache] hit/miss/invalidate receipts.
- Target br 11.5→~2-4ms. Stress test = FULL GP in emu (course transitions = invalidation storm).
- Quarantine: branch, NOT merged without user HW verdict.

## SHELVED (documented, branch preserved, do NOT delete)
`feat/3ds-gputransform` — GPU vertex transform WORKS (first in N64-port lineage, emu −62% vtx unlit,
220k draws zero-fallback) but HW-imperceptible (unlit-only phase-1 too small a slice) + venue-floor
texture-map breaks (PICA 24-bit uniform precision on ±32000-coord geometry). Killswitch-off by
default if ever revived. Phase-2 path (docs/research/gputransform-report.md): lit machine geometry
(~8ms prize) + large-coord CPU-fallback bounding for the floor. Only worth reviving if bridge cache
alone doesn't reach locked-60.

## Other remaining levers (if bridge cache insufficient)
Texture atlasing (137 machine-texture switches/frame, qualitative precedent); gputransform phase-2
(above); 30Hz-logic+interp (last resort, changes feel — gdx_interp infra exists).

## Backlog (tiny)
#27 tunnel roof — one course, near-plane/reversed-depth clip suspect, deprioritized (user: "looks ok").

## Hard-won gotchas (burned in)
- Regenerating a decomp patch: diff against the FULL-STACK base as a PURE DELTA, else it double-
  carries neighboring patches' hunks (rival-detail bit us TWICE — fix pattern: diff a clean-stack
  worktree's file vs the feature worktree's file). Verify with clean-submodule roundtrip apply.
- Smoke/[race-dl] detection needs ini `diag_audio=1` (or verbose=1) — gated since the leak fix.
- Emulator singleton: /tmp/azahar.lock (mkdir, touch ~25s, rm -rf); pkill -9 -i azahar + pgrep-empty
  before launch (stale-SD-cache hazard reloads old ini).
- Agents park on stale monitor timers post-completion — normal, ignore.
- gputrace INFLATES the HW profile (profop svc ticks) — true-fps runs use gputrace=0.
- UPSTREAM candidate: lus-cc-key-uninit-shader-id.patch (uninitialized ColorCombinerKey::shader_id —
  affects desktop LUS too, masked by stable stack layout; worth a PR to Kenix3/libultraship).

## SD loop
Card mounts /Volumes/NO NAME. Copy 3dsx to 3ds/, write 3ds/gdiffuser/gdiffuser.ini, rm log.txt,
sync, `diskutil eject "/Volumes/NO NAME"`. Read back 3ds/gdiffuser/log.txt.
